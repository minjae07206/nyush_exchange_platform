
export default function OpenToNegotiateFlagBadge() {
    let colorClass = "bg-green-600";

    return (
        <span className={`rounded-md mt-1 p-1 text-xs text-white ${colorClass}`}>
            Open to negotiate
        </span>
    );
}
