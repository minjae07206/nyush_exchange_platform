
export default function OpenToNegotiateFlagBadge() {
    let colorClass = "bg-green-600";

    return (
        <span className={`rounded-md ml-1 mr-1 mb-1 p-1 text-xs text-white ${colorClass}`}>
            Open to negotiate
        </span>
    );
}
